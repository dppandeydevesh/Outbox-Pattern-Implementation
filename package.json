{
  "name": "outbox-pattern-implementation",
  "version": "1.0.0",
  "description": "Full-stack Outbox Pattern with Express, MongoDB, at-least-once delivery, and idempotent publishing",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "process-outbox": "bash scripts/process_outbox.sh",
    "migrate": "bash scripts/migrate_outbox.sh",
    "health": "bash scripts/health_check.sh",
    "install-hooks": "cp git-hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit && echo 'Git hooks installed'"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.3.4",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
